---
description: Nuxt 模板须遵守统一设计系统（排版、主题 token、shadcn-vue、AppMain 宽度 B）
globs: "**/*.{vue,ts}"
---

# 设计系统（执行规则）

完整说明见 **`docs/design-system.md`**（工程根即本 Nuxt 项目根目录时路径如上）。**新增或显著调整**页面/组件的**排版、间距、栅格、圆角、字体层级**前，**必须先阅读**该文档 **第 4–9 节**；**`.mdc` 不能替代**长文。以下条目为必须遵守的硬规则摘要。

## 权威源

- 颜色与全局样式：**只认** `app/assets/css/main.css`；不写裸 hex / 随意 `gray-*` / `zinc-*` 替代语义 token。
- 组件：交互与版式**优先** `@/components/ui/*`；类名合并用 **`cn()`**。

## 布局与壳层

- 认证后台：沿用 **`SidebarProvider` + `AppSidebar` + `SidebarInset`（带 `@container/content`）**，不要随意第二套顶栏/侧栏。
- 主内容：**仅用** `AppMain` 作为 `#main-content` 包裹认证内页正文；须保留 **宽度 B**：类名中包含  
  `@7xl/content:mx-auto @7xl/content:w-full @7xl/content:max-w-7xl`，以及 `p-4 md:p-6`、`gap-4 md:gap-6`。

## 语义与暗色

- 背景/文字/边框：**`bg-background` `text-foreground` `border-border`** 等语义类；次要文字 **`text-muted-foreground`**。
- 暗色：**仅**通过 **`html` 的 `.dark`**；禁止为单页自造一套 palette。

## 图标与字体

- 图标：**Lucide**（`lucide-vue-next`）；行内常用 **`size-4`**。
- 字体：**Inter** / `font-sans`，勿引入未文档化的新字体栈。

## 禁止

- 裸 `div` 当按钮、跳过 shadcn 表单控件硬写样式。
- 删除或改丢 **`id="main-content"`**（除非设计文档已更新并同步本文）。

## 验收

- 改动后在**工程根目录**执行 **`pnpm run build`** 应通过；宽屏主内容区应对齐 **max-w-7xl** 居中行为。
